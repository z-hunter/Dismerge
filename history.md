# История версий Dismerge (current version = 0.0.9)

## v0.0.9 - Система спрайтов для фишек

### 🎮 **Новая функциональность**
- **Кастомные спрайты для фишек**: Каждая эволюционная цепочка может иметь собственный атлас со спрайтами
- **Автоматическое определение типа фишки**: Система автоматически определяет, есть ли у фишки уникальный спрайт
- **Умное отображение подписей**: Подписи уровней показываются только для фишек без уникальных спрайтов
- **Цветовая дифференциация**: Первые 6 эволюционных цепочек получают уникальные цвета для подписей

### 🔧 **Технические изменения**
- **Новая архитектура визуализации**: Переработана система отображения фишек с поддержкой кастомных спрайтов
- **Флаг `has_custom_sprite`**: Каждая фишка знает, есть ли у неё уникальный спрайт
- **Упрощенная логика цветов**: Убрана сложная логика проверки атласов, цвета назначаются по порядку
- **Оптимизированная функция `update_level_visual`**: Теперь работает только для фишек без уникальных спрайтов

### 🎨 **Система спрайтов**
- **Формат атласов**: `{evo_id}.atlas` (например, `GEN_P.atlas`)
- **Формат спрайтов**: `{evo_id}-{level}` (например, `GEN_P-1`, `GEN_P-2`)
- **Автоматическое переключение**: При получении сообщения `set_sprite` фишка автоматически скрывает подпись уровня
- **Fallback на дефолтный спрайт**: Если спрайт не найден, используется стандартный токен с подписью

### 📋 **API для спрайтов**
```lua
-- Установка кастомного спрайта
msg.post(token_id, "set_sprite", {
    atlas_id = "GEN_P",
    sprite_name = "GEN_P-1"
})

-- Автоматическое управление подписями
-- Если has_custom_sprite = true: подпись скрыта
-- Если has_custom_sprite = false: показывается подпись с цветом
```

### 🎯 **Логика отображения**
- **Фишки с уникальными спрайтами**: Подпись уровня заменяется на пробел, спрайт переключается на кастомный
- **Фишки без уникальных спрайтов**: Показывается подпись уровня с цветом из палитры (красный, зеленый, синий, желтый, пурпурный, голубой)
- **Автоматическое определение**: Система сама определяет тип фишки при инициализации

### 🔧 **Рефакторинг кода**
- **Упрощение `evolution_tables.lua`**: Убрана сложная логика проверки атласов, упрощена функция `get_chain_color`
- **Оптимизация `token.script`**: Убраны избыточные проверки и логирование, добавлен флаг `has_custom_sprite`
- **Централизованное логирование**: Использование `debug_logger` вместо `print()` в `evolution_tables.lua`

### 📊 **Цветовая палитра**
```lua
local colors = {
    vmath.vector4(1, 0, 0, 1),    -- Красный (1-я цепочка)
    vmath.vector4(0, 1, 0, 1),    -- Зеленый (2-я цепочка)
    vmath.vector4(0, 0, 1, 1),    -- Синий (3-я цепочка)
    vmath.vector4(1, 1, 0, 1),    -- Желтый (4-я цепочка)
    vmath.vector4(1, 0, 1, 1),    -- Пурпурный (5-я цепочка)
    vmath.vector4(0, 1, 1, 1),    -- Голубой (6-я цепочка)
}
```

### 🎯 **Результат**
- ✅ Система спрайтов для фишек полностью реализована
- ✅ Автоматическое определение типа фишки работает корректно
- ✅ Подписи уровней показываются только для нужных фишек
- ✅ Цветовая дифференциация работает для первых 6 цепочек
- ✅ Код стал проще и эффективнее
- ✅ Поддержка кастомных атласов без изменения основной логики

### 📚 **Документация**
- Обновлена техническая спецификация с описанием системы спрайтов
- Документирован API для установки кастомных спрайтов
- Описаны принципы работы системы отображения
- Добавлены примеры конфигурации атласов

## v0.0.8 - Централизованная система логирования

### 🔧 **Технические изменения**
- **Полная замена отладочного вывода**: Весь `print()` заменен на модуль логирования `debug_logger.lua`
- **Централизованное управление логами**: Единая система для всех типов отладочных сообщений
- **Фильтрация по типам**: Возможность включать/отключать различные категории логов
- **Ограничение по времени**: Предотвращение спама логов через throttling

### 📋 **Обновленные модули**
- **`scripts/generator_config.lua`**: Все отладочные сообщения переведены на `debug_logger`
- **`scripts/evolution_tables.lua`**: Логирование загрузки и отладки эволюционных цепочек
- **`scripts/csv_parser.lua`**: Обработка ошибок и информационные сообщения
- **`scripts/field_config.lua`**: Валидация и отладочный вывод конфигурации поля
- **`test/test_generator_indicators.lua`**: Тестовые сообщения переведены на модуль логирования
- **`scripts/debugger/debugger.lua`**: Отладочные функции для тестирования механики

### 🎯 **Типы логирования**
```lua
-- Ошибки (всегда включены)
debug_logger.log_error("Error message")

-- Важные события (с ограничением по времени)
debug_logger.log_important("Important event", throttle_seconds)

-- Информационные сообщения (для инициализации)
debug_logger.log_init("Info message")

-- Логи с ограничением по времени
debug_logger.log_with_throttle("message_type", "Message", throttle_seconds)
```

### 🔧 **Настройки логирования**
```lua
-- Управление настройками
debug_logger.set_log_setting("PROGRESS_INDICATOR", false)  -- Отключить логи индикаторов
debug_logger.set_log_setting("GENERATOR_CONFIG", false)    -- Отключить логи генераторов
debug_logger.set_log_setting("IMPORTANT", true)            -- Включить важные сообщения

-- Получение текущих настроек
local settings = debug_logger.get_log_settings()
```

### 📊 **Преимущества новой системы**
- **Централизованное управление**: Все логи проходят через один модуль
- **Гибкая настройка**: Можно включать/отключать различные типы логов
- **Предотвращение спама**: Throttling для часто повторяющихся сообщений
- **Категоризация**: Логи разделены по типам и важности
- **Профессиональный подход**: Замена примитивного `print()` на структурированную систему

### 🎯 **Результат**
- ✅ Весь отладочный вывод переведен на модуль логирования
- ✅ Система стала более управляемой и профессиональной
- ✅ Возможность тонкой настройки уровней логирования
- ✅ Предотвращение засорения консоли избыточными сообщениями
- ✅ Единообразный формат всех логов в проекте

## v0.0.7 - Полная имплементация генераторов по GDD

### 🎮 **Новая функциональность**
- **Полная система генераторов**: Реализованы все типы генераторов согласно GDD
- **Ручные генераторы**: Активация по клику, перезарядка, ограниченная емкость
- **Автоматические генераторы**: Автоматическая генерация в соседние клетки
- **Комбинированные генераторы**: Поддержка обоих типов на одной фишке
- **Одноразовые генераторы**: Генераторы с ограниченным количеством циклов

### 🔧 **Технические изменения**
- **Визуальные иконки генераторов**: Анимированные иконки для всех типов генераторов
- **Двойная система индикаторов**: Отдельные индикаторы для ручных и автоматических генераторов
- **Модульная архитектура индикаторов**: Переиспользуемый модуль круговых индикаторов прогресса
- **Улучшенная система дропа**: Разные стратегии для ручных и автоматических генераторов

### 🎯 **Система индикаторов**
- **Ручные генераторы**: Синие индикаторы в правом верхнем углу ячейки
- **Автоматические генераторы**: Зеленые индикаторы в левом верхнем углу ячейки
- **Независимая работа**: Каждый тип индикатора работает независимо от другого
- **Динамическое создание/удаление**: Индикаторы создаются и удаляются по мере необходимости

### 🎨 **Визуальные элементы**
- **Анимированные иконки**: Иконки генераторов с анимацией вращения
- **Цветовая дифференциация**: Синий для ручных, зеленый для автоматических генераторов
- **Позиционирование**: Точное размещение в углах ячеек без перекрытия
- **Z-координаты**: Правильное отображение поверх игрового поля

### 📊 **Конфигурация генераторов**
- **CSV-конфигурация**: Гибкая настройка через `config/gen.csv`
- **Параметры емкости**: Настройка количества генерируемых фишек
- **Времена перезарядки**: Отдельные таймеры для ручных и автоматических генераторов
- **Ограничения одноразовых**: Настройка количества циклов через `Dispose_after`

### 🐛 **Исправленные баги**
- **Дублирование индикаторов**: Автоматические генераторы больше не показывают оба типа индикаторов
- **Неправильный дроп**: Автоматические генераторы используют ближний дроп, ручные - обычный
- **Синхронизация анимаций**: Каждый генератор имеет независимое состояние анимации
- **Очистка ресурсов**: Корректное удаление всех типов индикаторов при завершении игры

### 📋 **API и интерфейсы**
```lua
-- Создание индикатора генератора
show_generator_indicator(self, generator_id, token_key, token, progress, type)

-- Управление индикаторами
remove_generator_indicator(self, token_key)           -- Ручные генераторы
remove_generator_auto_indicator(self, token_key)      -- Автоматические генераторы

-- Получение прогресса
get_generator_reload_progress(self, token_key)        -- Ручные генераторы
get_generator_auto_reload_progress(self, token_key)   -- Автоматические генераторы
```

### 🎯 **Результат**
- ✅ Полная имплементация генераторов согласно GDD
- ✅ Визуальные иконки и индикаторы для всех типов
- ✅ Корректная работа ручных и автоматических генераторов
- ✅ Поддержка комбинированных и одноразовых генераторов
- ✅ Независимые системы индикаторов для разных типов
- ✅ Оптимизированная архитектура с переиспользуемыми модулями

### 📚 **Документация**
- Обновлена техническая спецификация с описанием системы генераторов
- Документированы все API функции для работы с индикаторами
- Описаны принципы работы двойной системы индикаторов
- Добавлены примеры конфигурации и использования

## v0.0.6 - Модульный индикатор прогресса и рефакторинг

### 🎮 **Новая функциональность**
- **Модульный круговой индикатор прогресса**: Создан переиспользуемый модуль для отображения прогресса
- **ООП API для индикаторов**: Простой интерфейс для создания и управления индикаторами
- **Поддержка множественных индикаторов**: Возможность размещения нескольких независимых индикаторов на сцене

### 🔧 **Технические изменения**
- **Рефакторинг системы игрового поля**: Улучшена архитектура и структура кода
- **Модульная архитектура индикаторов**: Отдельные файлы для коллекции, скрипта и factory
- **ООП-модуль управления**: `scripts/progress_indicator_module.lua` с простым API
- **Удаление устаревших файлов**: Очистка от тестовых и неиспользуемых файлов

### 📋 **API модульного индикатора**
```lua
local Indicator = require "scripts.progress_indicator_module"

-- Создание индикатора
local ind = Indicator:new(position, scale)

-- Управление
ind:set_progress(0.75)  -- 0.0 - 1.0
ind:set_color(vmath.vector4(1, 0, 0, 1))  -- RGBA
ind:destroy()  -- Удаление
```

### 🎯 **Результат**
- Индикаторы можно создавать программно в любом месте экрана
- Поддержка настройки размера и цвета
- Независимая работа множественных индикаторов
- Чистая архитектура с разделением ответственности

### 🐛 Известные проблемы
- **Визуальный баг с отображением**: Индикатор может отображаться за фишкой генератора после её переноса
- **Причина не выяснена**: Проблема связана с Z-координатами, но точная причина не определена

### 📁 **Структура модуля**
```
modules/progress_indicator/
├── progress_indicator.collection     # Коллекция индикатора
├── progress_indicator.script         # Скрипт логики
└── indicator_factory.collectionfactory # Factory для создания
```

### 📚 **Документация**
- Обновлена техническая спецификация с описанием модуля
- Добавлены примеры использования и API
- Документированы принципы архитектуры

## v0.0.5
### 🐛 Исправленные баги
- **Механика выделения клеток**: Исправлена система выделения клеток поля, которая не работала корректно

#### Суть проблемы
Механика выделения клеток имела несколько критических проблем:
1. **Неправильные пути к объектам**: Ячейки отправляли сообщения на несуществующий объект board
2. **Потеря выделения при перетаскивании**: Выделение не сохранялось и не восстанавливалось после операций с фишками
3. **Неправильная обработка состояний**: Логика восстановления выделения работала некорректно

#### Решение
**Исправление путей к объектам:**
- Изменен путь в `item_script.script`: `msg.post("/board", ...)` → `msg.post("main:/board_factorys#board", ...)`
- Исправлен путь в `debugger.lua` для отладочных сообщений

**Улучшение логики выделения:**
- Добавлено поле `pre_drag_selection` для сохранения состояния выделения перед перетаскиванием
- Исправлена функция `start_drag()` - теперь создается копия объекта выделения
- Улучшена функция `perform_move()` - выделение перемещается вместе с фишкой
- Исправлена функция `perform_merge()` - выделение переходит на новую фишку после мерджа
- Улучшена функция `perform_return()` - выделение восстанавливается при возврате фишки
- Добавлена функция `check_selection_after_drag()` для проверки состояния выделения после операций

**Добавлена отладочная информация:**
- Подробное логирование всех операций с выделением
- Отслеживание ID объектов при отправке сообщений
- Диагностика координат и расстояний в функции `find_cell_at_position`

#### Результат
Теперь механика выделения работает корректно:
- ✅ Выделение сохраняется при начале перетаскивания
- ✅ Выделение перемещается вместе с фишкой при перемещении
- ✅ Выделение переходит на новую фишку при мердже
- ✅ Выделение восстанавливается при возврате фишки на исходную позицию
- ✅ Сообщения между объектами работают корректно

### 📋 Технические детали
```lua
-- Сохранение выделения перед перетаскиванием
if self.selected_cell then
    self.pre_drag_selection = { x = self.selected_cell.x, y = self.selected_cell.y }
end

-- Восстановление выделения при возврате
if self.pre_drag_selection and 
   self.pre_drag_selection.x == self.dragged_from.x and 
   self.pre_drag_selection.y == self.dragged_from.y then
    select_cell(self, self.dragged_from.x, self.dragged_from.y)
end

-- Перемещение выделения вместе с фишкой
if self.pre_drag_selection and 
   self.pre_drag_selection.x == self.dragged_from.x and 
   self.pre_drag_selection.y == self.dragged_from.y then
    select_cell(self, target_cell.x, target_cell.y)
end
```

## v0.0.4 - Генераторы с ручной активацией

### 🎮 **Новая функциональность**
- **Активация генераторов кликом**: Генераторы активируются при любом клике на клетку с генератором
- **Система вероятностей**: Реализован алгоритм выбора фишки по вероятностям из GDD
- **Упрощенная логика**: Любое выделение клетки с генератором считается активацией

### 🔧 **Технические изменения**
- **`generator_config.get_random_manual_output()`**: Функция для получения случайной фишки по вероятностям
- **`activate_generator()`**: Функция активации генератора с выводом результата в консоль
- **Интеграция в `select_cell()`**: Проверка генератора встроена в механизм выделения ячеек
- **Инициализация RNG**: Добавлен `math.randomseed(os.time())` для корректной работы вероятностей
- **Упрощение логики**: Убрана сложная логика определения тапа - теперь любое выделение генератора = активация

### 📋 **API генераторов**
- **`is_generator(token_id)`**: Проверка, является ли токен генератором
- **`get_random_manual_output(generator_id)`**: Получение случайной фишки по вероятностям
- **Структура генератора**: Добавлены поля `current_capacity`, `is_reloading` для состояния

### 🎯 **Результат**
- При клике на генератор в консоль выводится ID сгенерированной фишки в формате "evo_id-level"
- Система перетаскивания не нарушена
- Вероятности работают согласно алгоритму из GDD
- Логика стала проще и надежнее

### 🐛 Исправленные баги
- **Избыточные отладочные сообщения**: Убраны сообщения, засоряющие лог во время перетаскивания фишек
 